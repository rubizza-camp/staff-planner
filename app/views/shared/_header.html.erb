<nav class="navbar has-background-white">
  <div class="navbar-brand">
    <a class="navbar-item" href="/">
      <img src="/staff_planner.png" alt="logo" width="112" height="28" />
    </a>
  </div>
  <div class="navbar-end">
    <div class="navbar-item">
      <% if current_account && @company&.persisted? %>
        <%= form_with url: switch_companies_path, method: 'post' do |form| %>
          <div class="select">
            <%= form.collection_select :company_id, @account_companies, :id, :name, {selected: @company.id}, { onchange: 'this.form.submit();'} %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</nav>
