<nav class="navbar has-background-white">
  <div class="navbar-brand">
    <a class="navbar-item" href="/">
      <img src="/staff_planner.png" alt="logo" width="112" height="28" />
    </a>
    <% if current_account && @company %>
      <%= form_with url: company_switch_path, method: 'post', remote: true do |form| %>
        <%= form.collection_select :id, @account_companies, :id, :name, {}, { onchange: 'this.form.submit();'} %>
      <% end %>
    <% end %>
  </div>
</nav>
